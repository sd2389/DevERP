# orders/urls.py
from django.urls import path
from . import views

app_name = 'orders'

urlpatterns = [
    # Since views.py is empty, we'll start with a placeholder if needed
    # path('', views.order_list, name='order_list'),
]